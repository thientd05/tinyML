"""Model families (rf, svm, cnn, lstm) and their shared analytical cost model.

Each family is its own subpackage (sweep grid, estimator/architecture, training,
CLI). Run a family end-to-end with ``python -m src.models.<family>``.
"""
