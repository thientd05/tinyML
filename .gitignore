# virtualenv
env/

# python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.ruff_cache/

# large artifacts, all regenerable:
#   dataset/ — MIT-BIH download + per-mode .npz cache (src/data/assembly.py)
#   model/   — checkpoints + per-model metrics JSON (tools/multiseed.py)
dataset/
model/

# generated by tools/make_report.py — only a template.
# The live report is docs/BaoCaoDuAn.docx, which IS tracked.
results/BaoCao_TinyML_ECG.docx

# everything outside the core pipeline (slides + their generators, dataset probe):
# kept locally, never pushed
non_core/

# editor / OS
.vscode/
.idea/
.DS_Store

# Claude Code workspace
CLAUDE.md
.claude/
