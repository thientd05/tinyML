# Core pipeline (PC training + analysis).
#
# PyTorch is NOT listed here on purpose: it must come from the CUDA wheel index,
# which pip cannot resolve from a plain requirements file. Install it separately:
#
#   pip install -r requirements.txt
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# (cu126 is forward-compatible with the GTX 1650 / driver CUDA 13.0 box this was
# developed on. On a CPU-only machine, drop the --index-url.)

numpy>=1.24,<2.2
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
pandas>=2.0
joblib>=1.3
wfdb>=4.1
PyWavelets>=1.5
matplotlib>=3.7
tqdm>=4.65

# tools/make_report.py — generates the .docx report template
python-docx>=1.1

# tools/read_serial.py — reads the ESP32 benchmark table off the serial port
pyserial>=3.5
